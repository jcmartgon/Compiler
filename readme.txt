In order to execute the program simply run the parser.exe file, the program will prompt the user to type the name of  a file with source code to compile and execute, make sure to type the name of the file with its extension as well as to place the file within the same directory and level as the parser.exe file. The folder comes with several test cases, all of them .txt files with the prefix "test", feel free to try them out.